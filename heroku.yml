setup:
  config: {}
build:
  packages:
    - binutils
    - libproj-dev
    - gdal-bin
  languages:
    - nodejs
    - python
release:
  command:
    - python manage.py migrate
run:
  web: gunicorn jarrett.asgi --log-file -
